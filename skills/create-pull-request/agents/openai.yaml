interface:
  display_name: "Create Pull Request"
  short_description: "日本語PRと変更前後のMermaid構造図を整える"
  default_prompt: "Use $create-pull-request to draft a Japanese PR with the appropriate before/after Mermaid overview."
